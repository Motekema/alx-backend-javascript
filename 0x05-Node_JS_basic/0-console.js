/**
 * Return a message on the STDOUT.
 * @param {String} msg a message to display.
 * @author Motekema Mahlanya <https://github.com/Motekema>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
